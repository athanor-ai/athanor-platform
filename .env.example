# =============================================================================
# Athanor Platform — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your Supabase project values.
# See: https://supabase.com/docs/guides/getting-started/quickstarts/nextjs

# ---------------------------------------------------------------------------
# PUBLIC (safe to expose to the browser via NEXT_PUBLIC_ prefix)
# ---------------------------------------------------------------------------
# These are embedded in the client JS bundle and visible to end users.
# The anon key has limited permissions enforced by RLS policies.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ---------------------------------------------------------------------------
# SERVER-ONLY — NEVER prefix with NEXT_PUBLIC_
# ---------------------------------------------------------------------------
# SECURITY: The service role key bypasses Row Level Security and has full
# database access. It must ONLY be used in server-side code (API routes,
# Server Actions, server components) and must NEVER be exposed to the
# browser, client components, or frontend bundles.
#
# Exposing this key would allow any user to bypass tenant isolation,
# read/write all organizations' data, and access encrypted credentials.
#
# In Vercel, add this as a "Sensitive" environment variable and restrict
# it to the "Production" and "Preview" environments.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ---------------------------------------------------------------------------
# PLATFORM-OWNED API KEYS (Athanor internal use only)
# ---------------------------------------------------------------------------
# Set ATHANOR_USE_PLATFORM_KEYS=true to enable Athanor's own API keys
# as fallback for evaluation runs. Omit for customer builds.
#
# These are used for baseline scoring and demo runs. Customer-provided
# keys always take priority. Keys are read server-side only.
# Credential encryption (32-byte hex = 64 chars)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CREDENTIAL_ENCRYPTION_KEY=your-64-char-hex-key-here

# ---------------------------------------------------------------------------
# EVALUATION RUNNER VM (Azure)
# ---------------------------------------------------------------------------
# These control the Azure VM used to execute evaluation runs.
# The VM is started on demand and deallocated after runs complete.
# AZURE_VM_SSH_TARGET=azureuser@<vm-ip>
# AZURE_VM_RESOURCE_GROUP=ENV-RUNNER
# AZURE_VM_NAME=standard-env-runner
# AZURE_SUBSCRIPTION_ID=<your-subscription>

# Cloudflare Access (for customer email management)
# CF_ACCESS_TOKEN=your-cloudflare-api-token
# CF_ACCOUNT_ID=your-account-id
# CF_APP_ID=your-access-app-id
# CF_POLICY_ID=your-email-policy-id

ATHANOR_USE_PLATFORM_KEYS=false

# Google AI (Gemini Flash, Gemini Pro)
# GOOGLE_API_KEY=AIza...

# Azure AI (Kimi K2.5, Mistral Large 3 via Azure-hosted endpoint)
# AZURE_AI_API_KEY=your-azure-ai-key
# AZURE_AI_API_BASE=https://your-endpoint.services.ai.azure.com/models

# OpenAI-compatible fallback (same Azure endpoint, different routing)
# OPENAI_API_KEY=your-key
# OPENAI_API_BASE=https://your-endpoint.services.ai.azure.com/models

# HuggingFace (for model hosting)
# HF_TOKEN=hf_...
